# Live data never goes into git: the data file and its daily backups.
# Sample data for tests lives in tests/, not here.
data/
# ...also when a data folder was picked in the wrong place (e.g. the tool folder itself)
mrt_data*.json
backups/
# ...except the big made-up demo file: it IS in git, to test on any clone (node tests/make-demo-data.js)
!demo-data/
!demo-data/mrt_data.json

# Scratch and OS noise
.DS_Store
_preview.html
.claude/
